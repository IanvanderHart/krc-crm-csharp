name: Deploy C# API

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Set HOME for NuGet
        run: |
          echo "HOME=/home/csharprunner" >> $GITHUB_ENV
          echo "DOTNET_CLI_HOME=/home/csharprunner" >> $GITHUB_ENV

      - name: Restore dependencies
        run: |
          cd MeterApi
          dotnet restore --no-cache --force

      - name: Publish
        run: |
          cd MeterApi
          dotnet publish -c Release -o ./publish --no-restore

      - name: Deploy via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "MeterApi/publish/*"
          target: "/home/csharp/app/MeterApi"
          strip_components: 2
          overwrite: true
          debug: true
          timeout: 120s

      - name: Fix permissions and restart service
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            sudo chown -R csharp:csharp /home/csharp/app/MeterApi
            sudo systemctl restart meterapi
            sleep 2
            sudo systemctl status meterapi --no-pager
          request_pty: true
          debug: true
